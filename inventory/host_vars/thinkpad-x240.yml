env_platform: omarchy

# Optional host-level overrides:
env_install_overrides:
  # Example: skip netbird on this host
  # netbird:
  #   skip: true

  # Example: if you ever decide one host should NOT install brave:
  # brave:
  #   skip: true
