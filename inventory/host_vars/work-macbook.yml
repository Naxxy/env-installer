env_platform: macos

# Optional host-level overrides:
# - per item: set skip true/false
# - or override package names
env_install_overrides:
  # Example: skip qutebrowser on this host even if supported
  # qutebrowser:
  #   skip: true

  # Example: force brave install even if catalog sets skip somewhere
  # brave:
  #   skip: false
